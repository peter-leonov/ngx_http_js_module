worker_processes  1;

pid /tmp/nginx-js.pid;
error_log /tmp/nginx-js.log warn;

events { worker_connections 256; }

http
{
	access_log off;
	
	server
	{
		listen       9090;
		server_name  localhost;
		
		location /test
		{
			js 'function (r) { r.sendString("js works!") }';
		}
	}
}